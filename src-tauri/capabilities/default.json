{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permisos para la ventana principal: comandos de la app y diálogo de selección de carpeta.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
